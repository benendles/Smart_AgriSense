# Smart AgriSense — shared service dependencies
# NOTE: torch + torchvision (CPU build) are installed separately in the
# Dockerfile from the PyTorch CPU index to keep the image small.
fastapi==0.137.1
uvicorn[standard]==0.49.0
pillow==12.0.0
numpy==2.3.5
python-multipart==0.0.32
paho-mqtt==2.1.0
pydantic==2.12.4
scikit-learn==1.9.0
requests==2.32.5
anthropic>=0.92.0          # advisory service — Claude reasoning over model outputs
