# Raspberry Pi edge agent dependencies.
# Core (works on any machine, incl. laptop for simulation):
requests==2.32.5
paho-mqtt==2.1.0
pillow==12.0.0

# Hardware-only (install on the real Pi with sensors/camera attached):
#   pip install picamera2 adafruit-circuitpython-dht adafruit-circuitpython-mcp3xxx
# DHT22 also needs:   sudo apt install libgpiod2
